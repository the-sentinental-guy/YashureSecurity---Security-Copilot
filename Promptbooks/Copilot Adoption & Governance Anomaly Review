Using the Microsoft Sentinel workspace <WorkspaceName>, query the CopilotActivity table over <Timeframe>. Generate an adoption summary: total interactions, distinct users, and a breakdown of CopilotInteraction (manual) vs CopilotForSecurityTrigger (automated) counts. Present as a table.
From the same data, list the top 10 users and top agents/skills by interaction count over <Timeframe>, ordered descending. Parse AgentName and, where needed, the LLMEventData field for skill attribution.
Identify governance-relevant configuration events in CopilotActivity over <Timeframe>: plugin lifecycle (EnableCopilotPlugin / CreateCopilotPlugin / disable), promptbook deletions, and workspace changes. For each, show Timestamp, ActorName, operation, and source IP. Order by Timestamp descending.
Flag anomalies: any CopilotInteraction where the parsed LLMEventData shows JailbreakDetected = true, any after-hours interaction spikes, and any administrative operation performed by a user who does not normally perform it. Present as a severity-ordered table (Critical/High/Medium) with a one-line rationale each.
Generate an executive-ready governance brief: adoption trend, uneven adoption across teams, the highest-severity anomalies with recommended follow-up, and whether any event warrants a Sentinel incident. Audience: security leadership, non-technical. Under 250 words.
